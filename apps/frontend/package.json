{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "dotenv -e ../../.env -- vite",
    "build": "tsc -b && vite build",
    "lint": "eslint",
    "preview": "tsc -b && vite preview"
  }
}
