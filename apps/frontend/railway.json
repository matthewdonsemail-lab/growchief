{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install -g pnpm@10.7.0 && npx pnpm install --frozen-lockfile && npx pnpm run build"
  },
  "$schema": "https://railway.app/railway.schema.json"
}