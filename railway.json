{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "NODE_OPTIONS=--max_old_space_size=4096 pnpm install --frozen-lockfile && NODE_OPTIONS=--max_old_space_size=4096 pnpm run build:backend"
  }
}
